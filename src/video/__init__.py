"""
src.video — Video I/O and processing utilities.

Modules
-------
reader          : Open video files and read frames with OpenCV.
writer          : Write annotated frames to an output video file.
metadata        : Extract video properties (FPS, resolution, duration, codec).
shot_detection  : Detect camera cuts and classify shot types (wide, close-up, replay).
"""
